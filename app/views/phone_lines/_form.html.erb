<%= form_with(model: phone_line, local: true) do |form| %>
  <% if phone_line.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(phone_line.errors.count, "error") %> prohibited this phone_line from being saved:</h2>

      <ul>
      <% phone_line.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number %>
  </div>

  <div class="field">
    <%= form.label :SIM_key %>
    <%= form.text_field :SIM_key %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
